#!/usr/bin/env bash

set -euo pipefail

releases=$(curl -s "https://api.github.com/repos/cartridge-gg/vrf/releases" | \
  grep '"tag_name":' | \
  sed -E 's/.*"v([^"]+)".*/\1/' | \
  sort -V)

echo "$releases"
